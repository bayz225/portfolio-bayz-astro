---
export interface Props {
    id: string;
    classes?: string;
}

const {id, classes} = Astro.props
---
<section id={id} class=`px-2 sm:px-[25px] md:px-[50px] lg:px-[100px] sm:pt-[50px] my-[150px] ${classes}`>
    <slot/>
</section>